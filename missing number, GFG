class Solution {
    public static int missingNumber(int n, int[] arr) {
       
        int tot=(n*(n+1))/2;
        int sum=0;
        for(int i=0;i<arr.length;i++)
        {
            sum+=arr[i];
            
        }
        return tot-sum;
    }
}
        
